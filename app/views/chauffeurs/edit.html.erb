<div id="container_p">
    <div class="notice">
        <% if flash[:notice] %>
            <div class="alert alert-success alert-dismissible show fade">
                <%= flash[:notice] %>
                <button type="button" class="close" data-dismiss="alert">
                    <span>&times;</span>
                </button>
            </div>
        <% end %>
    </div>
    <div class="d-flex justify-content-around row">
        <div class="col-11 col-sm-8 col-md-6">
            <h2 style="text-align: center; padding: 20px">Edit chauffeur</h2>
            <%= form_for(@chauffeur, url: chauffeur_path(@chauffeur), local: true, method: :patch) do |f| %>
                <%= f.label :name %>
                <%= f.text_field :name %>
                <br>
                <%= f.label :quartier %>
                <%= f.text_field :quartier %>
                <br>
                <%= f.label :parking_id %>
                <%= f.collection_select :parking_id, Parking.all, :id, :name, { prompt: "Seclectionné le parking" } %>
                <br>
                <%= f.label :telephone %>
                <%= f.number_field :telephone %>
                <br>
                <%= f.label :photo_permis %>
                <%= f.file_field :photo_permis %>
                <br>
                <%= f.label :photo_carte %>
                <%= f.file_field :photo_carte %>
                <br>
                
                <%= f.submit "Modifier", class: "btn btn-gray" %>
            <% end %>
        </div>
    </div>
</div>